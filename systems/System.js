class System {
  init() {
    // Used to initialize any settings or global listeners needed by the system
  }

  update() {
    // Called every frame, overwritten by extended classes
  }
}

export default System;
